# Deployment Automation Scripts

This repository contains simple scripts and a tiny Node.js app used for CI/CD demonstrations.

Files:
- `backup.sh` — create a tar.gz backup of a folder
- `check.sh` — quick project check
- `Dockerfile` — builds a minimal Node.js image
